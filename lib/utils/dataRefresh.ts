// Stub - data refresh utility for MO Sell standalone
export function triggerActionRefresh(action: string, data?: Record<string, unknown>) {
  console.log('MO Sell standalone - action refresh:', action, data);
}
